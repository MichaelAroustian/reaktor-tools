[project]
name = "reaktor"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.26.0",
]

[project.optional-dependencies]
pdf-tools = [
    "pymupdf>=1.27.2.2",
]

